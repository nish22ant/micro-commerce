server:
  port: 8080
  
spring:
  data:
    mongodb:
      uri: mongodb+srv://root:root@cluster0.hqsqbhu.mongodb.net/product-service
  application:
    name: product-service
      
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true